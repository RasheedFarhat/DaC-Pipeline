# Direct dependencies (the curated source of truth).
# requirements.txt is the fully-pinned lock — regenerate it with:
#   pip install pip-tools && pip-compile requirements.in
# or keep using `pip freeze > requirements.txt` if you prefer.

# --- Compile & validate Sigma -------------------------------------------------
pySigma==1.4.0
sigma-cli==3.1.0
pySigma-backend-elasticsearch==2.1.1   # backend compilability check, scripts/validate_sigma.py
pysigma-pipeline-sysmon==2.0.0         # field pipeline for that same check
Jinja2==3.1.6                          # Wazuh XML template rendering
PyYAML==6.0.3

# --- Deploy to Wazuh ----------------------------------------------------------
requests==2.34.2
pydantic==2.13.4
pydantic-settings==2.15.0
python-dotenv==1.2.3                   # .env loading via pydantic-settings
tenacity==9.1.4                        # retry/backoff on transient API failures

# --- Dev / test / lint --------------------------------------------------------
pytest==9.1.1
responses==0.26.2                      # mock Wazuh API in tests
pre-commit==4.6.2
mypy==2.3.1
types-requests==2.33.0.20260518
types-PyYAML==6.0.12.20260518
